#show the list of remote repositories  (inria and nlafet)
git remote
#show the url of the remote repositories (inria and nlafet)
git remote -v

#pull from origin (inria)
git pull origin master
# pull from nlafet
git pull nlafet master

#push to nlafet
git push nlafet master

